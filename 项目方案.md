# 智能仓储管理系统 - 项目方案

## 一、项目概述
基于SpringBoot3 + Vue3的前后端分离电商/仓储管理系统，包含商品管理、订单管理、库存管理、用户权限等核心功能。

## 二、技术架构

### 后端技术栈
- **框架**: SpringBoot 3.x
- **ORM**: MyBatis-Plus
- **数据库**: MySQL 8.0
- **缓存**: Redis 7.x
- **安全**: Spring Security + JWT
- **文档**: Knife4j (Swagger增强)
- **定时任务**: Spring Task
- **构建**: Maven
- **容器**: Docker + Docker Compose

### 前端技术栈
- **框架**: Vue 3 + Composition API
- **构建**: Vite
- **UI**: Element Plus
- **路由**: Vue Router 4
- **状态管理**: Pinia
- **HTTP**: Axios
- **图表**: ECharts

## 三、项目结构

```
ds-system/
├── backend/                          # 后端SpringBoot项目
│   ├── src/main/java/com/ds/
│   │   ├── config/                   # 配置类
│   │   │   ├── RedisConfig.java
│   │   │   ├── SecurityConfig.java
│   │   │   └── SwaggerConfig.java
│   │   ├── controller/               # 控制器
│   │   ├── service/                  # 服务层
│   │   ├── mapper/                   # 数据访问层
│   │   ├── entity/                   # 实体类
│   │   ├── dto/                      # 数据传输对象
│   │   ├── vo/                       # 视图对象
│   │   ├── security/                 # 安全相关
│   │   │   ├── JwtTokenProvider.java
│   │   │   └── JwtAuthenticationFilter.java
│   │   ├── task/                     # 定时任务
│   │   └── exception/                # 异常处理
│   ├── src/main/resources/
│   │   ├── application.yml
│   │   ├── mapper/                   # MyBatis XML
│   │   └── db/
│   │       └── schema.sql           # 数据库脚本
│   ├── pom.xml
│   └── Dockerfile
│
├── frontend/                         # 前端Vue项目
│   ├── src/
│   │   ├── api/                      # API接口
│   │   ├── assets/                   # 静态资源
│   │   ├── components/               # 公共组件
│   │   ├── composables/              # 组合式函数
│   │   ├── layouts/                  # 布局组件
│   │   ├── router/                   # 路由配置
│   │   ├── stores/                   # Pinia状态
│   │   ├── views/                    # 页面视图
│   │   ├── utils/                    # 工具函数
│   │   ├── App.vue
│   │   └── main.js
│   ├── public/
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   └── Dockerfile
│
├── docker-compose.yml                # Docker编排
├── nginx.conf                        # Nginx配置
└── README.md
```

## 四、核心功能模块

### 1. 用户认证与权限 (JWT + RBAC)
- 用户登录/注册
- JWT Token生成与验证
- 角色管理（管理员、仓库员、普通用户）
- 菜单权限、按钮权限
- 接口权限控制

### 2. 商品管理
- 商品CRUD操作
- 商品分类管理
- 商品图片上传
- 商品库存管理
- **Redis商品缓存**（热点数据缓存）

### 3. 订单管理
- 订单创建与查询
- 订单状态流转
- **定时任务：订单超时自动取消**（30分钟未支付）
- 订单统计分析

### 4. 库存管理
- 入库/出库管理
- 库存盘点
- 库存预警
- 操作日志记录

### 5. 秒杀功能（Redis亮点）
- 秒杀商品管理
- **Redis预扣库存**（解决超卖）
- **Redis限流**（令牌桶算法）
- 秒杀结果异步处理

### 6. 数据统计
- 销售报表
- 库存周转率
- 用户行为分析
- ECharts可视化

## 五、数据库设计（核心表）

```sql
-- 用户表
CREATE TABLE sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    nickname VARCHAR(50),
    email VARCHAR(100),
    phone VARCHAR(20),
    avatar VARCHAR(255),
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 角色表
CREATE TABLE sys_role (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_name VARCHAR(50) NOT NULL,
    role_key VARCHAR(50) UNIQUE NOT NULL,
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 用户角色关联表
CREATE TABLE sys_user_role (
    user_id BIGINT NOT NULL,
    role_id BIGINT NOT NULL,
    PRIMARY KEY (user_id, role_id)
);

-- 商品表
CREATE TABLE product (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(200) NOT NULL,
    code VARCHAR(50) UNIQUE NOT NULL,
    category_id BIGINT,
    price DECIMAL(10,2),
    stock INT DEFAULT 0,
    description TEXT,
    image VARCHAR(255),
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 订单表
CREATE TABLE orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(50) UNIQUE NOT NULL,
    user_id BIGINT NOT NULL,
    total_amount DECIMAL(10,2),
    status TINYINT DEFAULT 0 COMMENT '0待支付 1已支付 2已发货 3已完成 4已取消',
    pay_time DATETIME,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 订单详情表
CREATE TABLE order_item (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_id BIGINT NOT NULL,
    product_id BIGINT NOT NULL,
    product_name VARCHAR(200),
    price DECIMAL(10,2),
    quantity INT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## 六、开发阶段划分

### 阶段一：基础搭建（第1-2天）
1. 后端项目初始化
2. 前端项目初始化
3. 数据库设计与创建
4. MyBatis-Plus配置
5. Redis配置
6. Docker环境配置

### 阶段二：用户认证（第3-4天）
1. 用户实体与Mapper
2. JWT工具类
3. Spring Security配置
4. 登录/注册API
5. 前端登录页面
6. 前端路由守卫

### 阶段三：商品管理（第5-6天）
1. 商品CRUD API
2. 商品分类管理
3. **Redis商品缓存实现**
4. 前端商品列表/详情页
5. 商品搜索功能

### 阶段四：订单管理（第7-8天）
1. 订单创建与查询API
2. **订单超时自动取消定时任务**
3. 前端购物车
4. 前端订单页面

### 阶段五：库存与秒杀（第9-10天）
1. 库存管理API
2. **Redis秒杀预扣库存**
3. **Redis限流实现**
4. 前端秒杀页面

### 阶段六：部署上线（第11-12天）
1. Dockerfile编写
2. Docker Compose编排
3. Nginx配置
4. 服务器部署测试
5. 线上环境验证

## 七、GitHub提交策略

### 分支管理
```
main              # 主分支，生产环境代码
├── develop       # 开发分支
├── feature/user-auth    # 功能分支：用户认证
├── feature/product      # 功能分支：商品管理
├── feature/order        # 功能分支：订单管理
└── feature/seckill      # 功能分支：秒杀功能
```

### Commit Message规范
```
feat: 添加用户登录API
fix: 修复商品库存扣减bug
style: 调整登录页面样式
docs: 更新README文档
refactor: 重构订单服务
test: 添加用户模块单元测试
chore: 配置Docker环境
```

### 提交频率
- 每完成一个独立功能点就提交
- 每个阶段完成后提交
- 修复bug后立即提交
- 代码审查后提交

## 八、与我配合方式

### 你负责：
1. 创建GitHub仓库
2. 按我指导初始化项目
3. 执行git命令提交代码
4. 提供数据库、Redis等配置信息
5. 测试功能并反馈问题

### 我负责：
1. 提供完整的代码实现
2. 指导每个步骤的操作
3. 解答技术问题
4. 提供配置文件模板
5. 代码审查建议

### 工作流程：
1. 我告诉你当前阶段需要做什么
2. 你创建文件并复制代码
3. 你测试功能是否正常
4. 你按规范提交到GitHub
5. 我审查并提供反馈
6. 进入下一个功能点